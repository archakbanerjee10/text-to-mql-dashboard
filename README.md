# text-to-mql-dashboard
A React and FastAPI web application that converts natural language questions into MongoDB Query Language (MQL) aggregation pipelines using Gemini, executing queries in real time with interactive dynamic charts and schema exploration.
